# TEMPLATE - copier en postgres-secret.yaml et remplir les vraies valeurs.
# Le vrai fichier postgres-secret.yaml est gitignore.
# En pratique deploy.sh cree ce secret via `kubectl create secret --from-literal`,
# donc ce template sert surtout de documentation des cles attendues.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: boutique
type: Opaque
stringData:
  POSTGRES_USER: boutique_app
  POSTGRES_PASSWORD: changeme-en-local
  POSTGRES_DB: boutique
